<div class="container">
  <div class="columns">
    <div class="column is-one-third" data-controller="search-form">

      <h1 class="title is-4">Other Players</h1>

      <p>These are players for EC2022 who have not pre-registered. This list is not visible publicly.
        Note that a player should be registered either here or in the pre-registered list, not both!</p>

      <p>Feel free to test and play around before Ropecon, this list will be cleared before EC starts.</p>

      <%= form_with(url: players_path, method: :get,
                    data: { turbo_frame: "players", search_form_target: "form" }) do |f| %>
        <div class="control has-icons-left mt-4">
          <%= f.search_field :query, placeholder: "Search names and VEKN numbers", autocomplete: "off",
                             class: "input is-link is-rounded",
                             "data-action": "click->search-form#clear input->search-form#search"
          %>
          <span class="icon is-left">
            <i class="fas fa-search"></i>
          </span>
        </div>
      <% end %>

      <div class="control mt-4">
        <%= button_to "Add new player", new_player_path, method: :get,
                      form: { data: { turbo_frame: "players" } },
                      class: "button is-primary"
            #"data-action": "click->search-form#clearinput"
        %>
      </div>

      <template id="clear_trigger"></template>
      <div id="crud_notify" class="mt-4"></div>

    </div>
    <div class="column">
      <%= render "players", players: @players, query: @query, page: @page %>
    </div>
  </div>
</div>
